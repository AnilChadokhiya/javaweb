package com.techforce.servlet;



public class PracticeJava8 {

	/**
	 * @param args
	 */
	public static void main(String[] args) {

		/*
		 * List<Integer> list = Arrays.asList(10,12, 45, 50, 80);
		 * 
		 * 
		 * Integer orElse = list.stream().filter( ls -> 12 == ls ).findAny().orElse(
		 * 13456 );
		 * 
		 * 
		 * System.out.println(orElse.hashCode());
		 * 
		 * 
		 * //result.forEach(System.out::println); /* result.forEach(rs ->{ //
		 * System.out.println(list.stream().filter(ls -> "10".equals(ls)
		 * ).findFirst().orElse(12);); });
		 * 
		 * //result.ifPresent( result );
		 * 
		 * LocalDate endofCentury = LocalDate.of(1997, 07, 26); LocalDate now =
		 * LocalDate.now();
		 * 
		 * Period diff = Period.between(endofCentury, now);
		 * 
		 * System.out.println( diff.getUnits());
		 * 
		 * System.out.printf("Difference is %d years, %d months and %d days old",
		 * diff.getYears(), diff.getMonths(), diff.getDays() ); }
		 * 
		 * 
		 * List<String> list = Arrays.asList( "Anil" , "Manish" , "Dhanraj" );
		 * 
		 * Predicate<String> isEven = i -> i .equals( "Manish" );
		 * 
		 * Predicate<String> isOdd = isEven.negate();
		 * 
		 * List<String> evenNumbers = list.stream().filter( isEven ).collect(
		 * Collectors.toList() );
		 * 
		 * List<String> oddNumbers = list.stream().filter( isOdd ).collect(
		 * Collectors.toList() );
		 * 
		 * System.out.println( evenNumbers ); System.out.println( oddNumbers );
		 */

	}

}
